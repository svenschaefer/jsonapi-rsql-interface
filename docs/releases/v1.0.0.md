# Release Notes - `v1.0.0` (Draft)

Status: draft (pre-tag)

## Release Identity

- Package: `jsonapi-rsql-interface`
- Target version: `1.0.0`
- Planned tag: `v1.0.0`
- Planned release stream:
  - Git tag + release commit
  - npm publish + propagation verification
  - external pre/post publish smoke verification

## Contract Baseline (GA)

- Compiler-style deterministic API:
  - `compileRequest(input)`
  - `compileRequestSafe(input)`
- Canonical pipeline:
  - `parse -> normalize -> type-check -> policy-check -> compile`
- JSON:API compatibility target:
  - query parameter surface only (`filter`, `sort`, `fields[...]`, `include`, `page[...]`)
- RSQL compatibility target:
  - core filter/operator semantics as documented in `README.md`
  - wildcard extension remains post-GA scoped (`v1.1.x`)

## Evidence Checklist (to fill at release time)

- Commit SHA: `TBD`
- CI gate (`npm run ci:check`): `TBD`
- Governance gate (`npm run gov:check`): `TBD`
- Runtime audit gate (`npm run audit:runtime`): `TBD`
- Pre-publish external smoke:
  - harness path: `C:\code\jsonapi-rsql-interface-smoke-test`
  - prepare command: `npm run smoke:external:prepare -- --version 1.0.0 --phase all --harness-dir "C:\code\jsonapi-rsql-interface-smoke-test" --harness-package jsonapi-rsql-interface-smoke-test`
  - command: `npm run smoke:external -- --phase pre --version 1.0.0 --harness-dir "C:\code\jsonapi-rsql-interface-smoke-test" --harness-package jsonapi-rsql-interface-smoke-test --package-name jsonapi-rsql-interface`
  - optional deterministic run id: add `--timestamp <YYYYMMDDTHHMMSSZ>`
  - resolution rule: if root harness path has no `package.json`, runner executes from installed harness package under `<harness-dir>/<timestamp>-<phase>-<version>/node_modules`
  - result: `TBD`
- Post-publish external smoke:
  - harness path: `C:\code\jsonapi-rsql-interface-smoke-test`
  - command: `npm run smoke:external -- --phase post --version 1.0.0 --harness-dir "C:\code\jsonapi-rsql-interface-smoke-test" --harness-package jsonapi-rsql-interface-smoke-test --package-name jsonapi-rsql-interface`
  - result: `TBD`

## Migration Note

- See `docs/MIGRATION_0.x_TO_1.0.0.md`.
